# CLAUDE.md

Agent instructions for this repo live in **[`AGENTS.md`](./AGENTS.md)** — read that file.

It is the vendor-neutral contract: required preflight, non-negotiables, edit surface, how to add a page, and the checks that must pass. This pointer exists so Claude Code finds its way there. Don't duplicate content here — a rule written twice drifts twice.
